# 🚀 CloudCode Session Manager - 运行说明

## 问题

本地 macOS Xcode 编译器配置有问题，导致 Rust 编译失败。这是系统问题，不是代码问题。

## 解决方案（只需一次）

### 方法 1：使用启动脚本（推荐）

1. **打开 macOS 终端**
   - 按 `Cmd + Space`
   - 输入 `Terminal`
   - 按 `Enter`

2. **运行脚本**
   ```bash
   bash /Users/mannix/Project/projectTerm/cloudcode-rust/RUN_ME.sh
   ```

3. **输入你的 Mac 密码**
   - 脚本会要求输入密码
   - 输入时不会显示字符，正常输入即可
   - 按 `Enter`

4. **等待应用启动**
   - 首次编译需要 30-60 秒
   - 之后会自动打开应用窗口

---

### 方法 2：手动运行命令

如果脚本不工作，手动运行这些命令：

```bash
# 1. 重置 Xcode（需要输入密码）
sudo xcode-select --reset

# 2. 进入项目目录
cd /Users/mannix/Project/projectTerm/cloudcode-rust

# 3. 清除旧文件
rm -rf src-tauri/target
rm -f src-tauri/Cargo.lock

# 4. 启动应用
npm run dev
```

---

## 应用启动后

一旦看到应用窗口打开（标题：CloudCode Session Manager），你就可以：

1. **创建项目** - 点击 "New Project"
2. **创建会话** - 在项目中点击 "New Session"
3. **打开终端** - 选择会话，点击 "Open in Terminal"

---

## 如果还是不行

如果上述方法不工作，你有两个选择：

### 选项 A：使用 GitHub Actions（推荐）
```bash
git init
git add .
git commit -m "CloudCode MVP"
git remote add origin https://github.com/YOUR_USERNAME/cloudcode-rust.git
git push -u origin main
```

然后 GitHub Actions 会自动编译，你可以下载生成的 DMG 文件直接运行。

### 选项 B：查看源代码
```bash
# 即使无法编译，你也可以查看完整的代码
cat src/App.tsx
cat src-tauri/src/main.rs
cat README.md
```

---

## 需要帮助？

- 项目位置：`/Users/mannix/Project/projectTerm/cloudcode-rust/`
- 文档：`README.md`, `QUICKSTART.md`, `PROJECT_SUMMARY.md`
- 所有代码都已完成并可以查看

